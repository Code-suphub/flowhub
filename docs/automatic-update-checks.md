# 自动检查更新

正式构建（含本地迭代版）启动后约5秒首次检查，之后在上一轮完成后等待10分钟再检查。慢请求不会重叠，休眠唤醒不会集中补发错过的检查。开发调试构建保持不检查更新。

每轮重新读取当前保存的`core.autoUpdateCheck`与`core.autoUpdateInstall`。自动检查默认开启；关闭后跳过该轮，重新开启无需重启。设置变更在下一轮生效。配置读取失败时跳过该轮并记录日志，不按默认开启处理读取失败。

手动检查、自动检查、下载和安装共用已有操作锁；有操作进行时自动检查跳过。已下载的更新包继续保留，不被周期查询覆盖。自动安装默认关闭；只有用户已有设置开启且发现/缓存了更新才下载或安装，下载失败不继续尝试安装。启动日志记录首次延迟5秒与周期600秒。

验证使用Tokio虚拟时钟，覆盖首次5秒、后续600秒、跳过检查后继续调度、慢请求结束后等待下一周期、跨越一小时不补发，以及设置默认值。不依赖访问GitHub，不用真实等待10分钟。

本地版本`0.1.9-local.20260909062229`已安装，启动日志确认周期600秒，首次真实检查在启动约5秒后发生并成功取得正式版信息。97项Rust测试及全部UI、预览、安全检查通过；隔离设置页面中关闭/重新开启自动检查均能保存，无页面异常。10分钟重复与休眠行为通过虚拟时钟验证，未以真实时间等待完整周期。本轮尚未发布新的GitHub正式版。
