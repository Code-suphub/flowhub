{
  "name": "flowhub-app",
  "productName": "FlowHub",
  "version": "0.1.0",
  "description": "uTools 风格的桌面启动器：全局搜索目录/网页，回车打开；可配置启动本地应用。",
  "scripts": {
    "start": "tauri dev",
    "dev": "tauri dev",
    "dev:web": "vite --config vite.config.mjs",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml && node --check ui/tauri-adapter.js && node --check ui/search.js && node --check ui/settings.js",
    "build": "node scripts/tauri-build.mjs",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "node scripts/tauri-build.mjs"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "sql.js": "^1.14.2"
  }
}
